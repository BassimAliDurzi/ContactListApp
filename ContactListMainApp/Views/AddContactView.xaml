<UserControl x:Class="ContactListMainApp.Views.AddContactView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ContactListMainApp.Views"
             xmlns:viewmodles="clr-namespace:ContactListMainApp.ViewModels"
             d:DataContext="{d:DesignInstance Type=viewmodles:AddContactViewModel}"
             
             mc:Ignorable="d"
             Background="White"
             d:DesignHeight="800" d:DesignWidth="500">


    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <TextBlock Text="{Binding Title}"></TextBlock>
            
        </StackPanel>

        <Button Grid.Row="1" Padding="8" Content="Go Back" Command="{Binding GoToContactsCommand}"/>

    </Grid>

</UserControl>
